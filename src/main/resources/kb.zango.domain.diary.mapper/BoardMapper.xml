<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kb.zango.domain.diary.mapper.BoardMapper">
    <insert id="insertBoard">
        INSERT INTO board (user_id, title, content, like_count, comment_count, regi_date)
        VALUES (#{user.userId}, #{title}, #{content}, #{likeCount}, #{commentCount}, #{regiDate})
    </insert>
</mapper>
