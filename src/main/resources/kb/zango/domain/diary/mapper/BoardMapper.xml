<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kb.zango.domain.diary.mapper.BoardMapper">
    <insert id="insertBoard" parameterType="kb.zango.domain.diary.board.entity.Board"
            useGeneratedKeys="true" keyProperty="boardId">
        INSERT INTO board (user_id, title, content, like_cnt, comment_cnt, regi_date, sc_id)
        VALUES (#{user.userId}, #{title}, #{content}, #{likeCnt}, #{commentCnt}, #{regiDate}, #{smallCategory.scId})
    </insert>

    <select id="findBoardById" resultType="kb.zango.domain.diary.board.entity.Board">
        SELECT * FROM board WHERE board_id = #{boardId}
    </select>

    <select id="findAllBoards" resultType="kb.zango.domain.diary.board.entity.Board">
        SELECT * FROM board ORDER BY regi_date DESC
    </select>

    <update id="updateBoard" parameterType="kb.zango.domain.diary.board.entity.Board">
        UPDATE board
        SET title = #{title}, content = #{content}, like_count = #{likeCnt},
            comment_count = #{commentCnt}, regi_date = #{regiDate}
        WHERE board_id = #{boardId}
    </update>

    <delete id="deleteBoard" parameterType="Long">
        DELETE FROM board WHERE board_id = #{boardId}
    </delete>
</mapper>

